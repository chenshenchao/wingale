<UserControl x:Class="Wingale.View.TaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wingale.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button Click="OnUpdate">Update</Button>
            </ToolBar>
            <ToolBar>
                <TextBox x:Name="keyword" Width="200"/>
                <Button Click="OnSearch">Search</Button>
            </ToolBar>
        </ToolBarTray>
        <ListView x:Name="table" GridViewColumnHeader.Click="OnSort" SelectionChanged="OnSelect">
            <ListView.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn Header="PID" DisplayMemberBinding="{Binding Id}"/>
                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding ProcessName}"/>
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</UserControl>
